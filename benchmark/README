

Steps to run benchmarking process, requires Maven:

1. Publish net.corda.pq.research containing prototype implementation to local Maven

2. With benchmark as cwd run:

    $ `mvn clean install`
    $ `java -jar ./target/benchmark.jar`

Note: running times for signing and verification times should be divided by the message
batch size (see implementation)